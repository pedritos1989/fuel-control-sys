{% extends '::base.html.twig' %}

{% block block('subheader') %}

{% block header 'user.label'|trans %}

{% block subheader 'user.list'|trans %}

{% block container -%}
    {%- embed '_smart_widget.html.twig' %}
        {% set config, collapse, refresh, remove = true, true, false, false %}
        {% block widgetheader %}
            {{ 'user.list'|trans }}
        {% endblock widgetheader %}

        {% block hidden_option_icon '<i></i>' %}

        {% block widgetbody %}
            <div class="row">
                <div class="col-md-12">
                    <a class="btn btn-primary btn-sm" href="{{ path('user_new') }}">
                        <i class="fa fa-plus-circle"></i> {{ 'actions.add'|trans }}</a>
                </div>
                <div class="col-md-12 separator-index">
                    {% include 'security/user/_list.html.twig' %}
                </div>
            </div>
        {% endblock widgetbody %}
    {%- endembed %}
{% endblock %}

{% block javascripts -%}
    <script type="text/javascript">
        $(document).ready(function () {
            $('#table-datatable').DataTable();
        });
    </script>
{% endblock %}
